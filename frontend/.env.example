# Where the app gets its data.
# Leave empty to run on the built-in localStorage mock (demo@checkly.dev / demo1234).
# Set to the backend base URL (no trailing slash) to use the real API.
VITE_API_URL=
